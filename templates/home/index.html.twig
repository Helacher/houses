{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}


<section class="regervation_part section_padding" style="margin-top: 100px;">
    <div class="container">
        <div class="row">
{% for item in items %}
<div class="col-3 mt-3">
 <div class="card bg-light mb-3" style="max-width: 20rem;">
  <div class="card-header"><img src="/uploads/images/products/{{item.image}}" alt="card-img-top" style="width: 100%; heigh:auto;"></div>
  <div class="card-body">
    <h4 class="card-title"><a href="{{ path('element',{'id': item.id })}}" {{ item.title }}>{{ item.title }}</a></h4>
    <p class="card-text">{{ item.city }}</p>
    <div classe"text-primary">{{ item.price | number_format(0,'',' ') }}</div></div>
  </div>
</div>
 {% endfor %}
  </div> </div>
 </section>
 </main>
{% endblock %}
